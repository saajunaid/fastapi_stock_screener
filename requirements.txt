# requirements.txt
# FINAL VERSION - Installs pandas-ta directly from GitHub to bypass PyPI issues.

fastapi
uvicorn[standard]
apscheduler
jinja2
python-multipart

pandas>=2.2,<3.0
numpy
PyYAML
alpaca-trade-api
scipy
python-dotenv

# Install pandas-ta directly from the main branch of its official GitHub repository
# using the git:// protocol to avoid authentication issues in the build environment.
pandas-ta @ git+git://github.com/twopirllc/pandas-ta.git@main

